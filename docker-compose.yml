version: '3.4'

networks:
    docker-net-vpn:
        external: true
services:
  spotsline-backend:
    container_name: spotsline-backend
    cap_add:
     - NET_ADMIN
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - ${NEST_PORT}:3000
    networks:
      - docker-net-vpn

